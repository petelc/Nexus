# Base URL of the Nexus frontend dev server
BASE_URL=https://localhost:3000

# Test user credentials (must exist in the database before running tests)
TEST_USER_EMAIL=testuser@nexus.dev
TEST_USER_PASSWORD=TestPass123!

# Optional: admin user for admin-specific tests (must have Admin role)
ADMIN_USER_EMAIL=admin@nexus.dev
ADMIN_USER_PASSWORD=AdminPass123!
